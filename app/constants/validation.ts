/**
 * Constants for validation rules.
 */

// Minimum length for a username
export const MIN_USERNAME_LENGTH = 4;

// Minimum length for a password
export const MIN_PASSWORD_LENGTH = 8;

// Maximum number of tags allowed in an input array
export const MAX_TAGS_INPUT_ARRAY_LENGTH = 5;

// Maximum length of a single tag in the input string
export const MAX_TAGS_INPUT_STRING_LENGTH = 20;
